# Workspace Instructions

- Manage Python dependencies and commands with `uv`.
- Prefer `uv sync` for environment setup, `uv add` for dependency changes, and `uv run python ...` for scripts and tests.
- Avoid plain `pip install` or bare `python` commands unless debugging the runtime itself.
